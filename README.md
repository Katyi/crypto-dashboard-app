# 🪙 Deai Crypto Score Dashboard

Этот проект представляет собой полнофункциональное веб-приложение для мониторинга и анализа ключевых метрик криптовалют. Приложение состоит из двух основных частей: бэкенда (API на NestJS) для сбора и обработки данных и фронтенда (Клиент на React) для визуализации.

Проект разработан в рамках демонстрации Full-Stack стека (T3 Stack / Modern Stack).

# 🚀 Технологический стек

Фронтенд (Client): React (Vite), Tailwind CSS, Recharts

Бэкенд (API): NestJS, TypeScript, Prisma ORM, @nestjs/schedule(модуль для запуска Cron-задач).

База данных: PostgreSQL

Сбор данных: CoinGecko API

Источник рыночных данных в реальном времени.

# 🛠️ Структура проекта

Проект разделен на две основные папки:

api/: Содержит бэкенд-приложение на NestJS. Отвечает за:

- Ежедневный сбор данных (через Cron).
- Расчет Deai Score (уникальный индекс).
- Сохранение метрик в PostgreSQL.
- Предоставление REST API для фронтенда.

client/: Содержит фронтенд-приложение на React/Vite. Отвечает за:

- Отображение главного дашборда.
- Запросы исторических данных через /api/metrics.
- Интерактивную визуализацию метрик.

# ⚙️ Настройка и запуск

1. Предварительные требования

- Node.js (v18+)
- npm или Yarn
- Docker (рекомендуется для локального запуска PostgreSQL)

2. Настройка базы данных и API (папка api/).

1. Перейдите в папку бэкенда:

   cd api

1. Установите зависимости:

   npm install

1. Создайте файл .env:

   Скопируйте пример из файла .env.example (если он есть) и укажите строку подключения к вашей базе данных PostgreSQL.

1. Разверните схему базы данных (Prisma Migrate):

   npx prisma migrate dev --name init

1. Запустите API в режиме разработки:

   npm run start:dev

   API будет доступен по адресу http://localhost:3000.

1. Настройка и запуск Фронтенда (папка client/)

1. Перейдите в папку клиента:

   cd client

1. Установите зависимости (включая react-is для Recharts):

   npm install
   npm install react-is

1. Запустите клиентское приложение:


    npm run dev

    Клиент будет доступен по адресу http://localhost:5173/ (или другому порту, указанному Vite).

    💡 Примечание: В режиме разработки (Dev) настроен прокси-сервер в vite.config.js, который автоматически перенаправляет запросы к API на http://localhost:3000.

# 🚢 Развертывание (Deployment)

Проект использует раздельное развертывание:

1. API (NestJS): Требует постоянного запущенного сервиса для работы Cron-заданий.


    - Рекомендуемые платформы: Render, AWS ECS, DigitalOcean App Platform.

2. Client (React/Vite): Статическое приложение.


    - Рекомендуемые платформы: Vercel, Netlify, Firebase Hosting.
